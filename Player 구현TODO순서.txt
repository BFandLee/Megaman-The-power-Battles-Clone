🗺️ 플레이어 TODO 정복 로드맵 (순서대로 진행하세요!)
[1단계: FSM 초기 뼈대 조립하기]
1.FSMComponent::Init() 으로 가서 new IdleState(this) 객체를 만들고 _states 맵에 등록(AddState)합니다. 그리고 초기 상태를 세팅합니다.

2. FSMComponent::ChangeState(name) 함수 내부를 채웁니다. (힌트: 기존 상태의 Exit() 호출 -> 상태 포인터 교체 -> 새 상태의 Enter() 호출)

[2단계: 로직 이사하기 (리팩토링)]
Player.cpp의 Update()에 길게 방치되어 있던 낡은 키보드 입력 코드와 좌표 이동 코드를 싹 다 잘라내기(Ctrl+X) 합니다.

IdleState.cpp의 Update() 안으로 가져와서 붙여넣습니다.
Player.cpp에는 낡은 switch 문이 남아있을 텐데, 과감하게 전부 지워버려서 Player.cpp를 텅텅 비우고 깔끔하게 만듭니다!

[3단계: 상태 쪼개기 및 확장]
게임을 켜서 일단 Idle 상태에서 이동이 정상 작동하는지 눈으로 확인합니다.

작동을 확인했다면, 이제 MoveState, JumpState 클래스를 새롭게 만들고 Init()에 추가 등록합니다.

IdleState에 뭉쳐두었던 로직들을 각 상태의 성격에 맞게 쪼개어 분리하고, 키보드 입력에 따라 서로 ChangeState()가 호출되며 자연스럽게 상태가 넘어가도록 만듭니다.

[4단계: 무기 시스템 완성 (마지막)]
FSM(하체)이 완벽해졌으니 상체(공격)를 완성할 차례입니다.
WeaponComponent::Init()에서 new Buster()를 장착하고, 발사 키를 누르면 총알 오브젝트가 생성되도록 코딩합니다.